import { styled,Box } from "@mui/material";
const Section = styled(Box)(({theme})=>{

})

const UserCard =  styled(Box)(({theme})=>({
    display:'flex',
    justifyContent:'center',
    alignItems:'center',
    gap:'1rem',
    padding:'2rem'
}))

const UsersList =  styled(Box)(({theme})=>{

})

export {Section,UserCard,UsersList};